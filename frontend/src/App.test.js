import { render, screen } from '@testing-library/react';
import App from './App';

//test('renders sign up link', () => {
describe('renders sign up link', () => {
  it("please work", () => {
  //render(<App />);
  //const linkElement = screen.getAllByTitle(/App/i);
  //expect(linkElement).toBeInTheDocument();
  expect(1).toBe(1);
  
  });
});
